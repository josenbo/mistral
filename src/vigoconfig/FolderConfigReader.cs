using vigobase;

namespace vigoconfig;

public static class FolderConfigReader
{
    public static FolderConfig Parse(string content, FileHandlingParameters defaultFileHandlingParams, StandardFileHandling deployConfigRule, StandardFileHandling finalCatchAllRule)
    {
        return new FolderConfig();
    }
}